<?xml version="1.0" encoding="UTF-8"?>

<beans:beans xmlns="http://www.springframework.org/schema/mvc"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:beans="http://www.springframework.org/schema/beans"
    xmlns:context="http://www.springframework.org/schema/context"
    xmlns:tx="http://www.springframework.org/schema/tx"
    xsi:schemaLocation="
        http://www.springframework.org/schema/mvc
        https://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/beans
        https://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        https://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/tx
        https://www.springframework.org/schema/tx/spring-tx.xsd">

    <!-- Spring MVC Annotation 사용 -->
    <annotation-driven />

    <!-- CSS / JS / 이미지 등 정적 리소스 -->
    <resources mapping="/resources/**"
               location="/resources/" />

    <!-- JSP View Resolver -->
    <beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <beans:property name="prefix" value="/WEB-INF/views/" />
        <beans:property name="suffix" value=".jsp" />
    </beans:bean>

    <!-- Controller 패키지 Scan -->
    <context:component-scan base-package="kr.or.tradelog" />

    <tx:annotation-driven transaction-manager="transactionManager" />
    
    <context:property-placeholder location="classpath:api-secret.properties" />
    
	<!-- 로그인 Interceptor 설정 -->
	<interceptors>
	    <interceptor>
	
	        <!-- Interceptor를 적용할 요청 주소 -->
	        <mapping path="/dashboard"/>
	        <mapping path="/trade/**"/>
	        <mapping path="/rules/"/>
	        <mapping path="/rules/**"/>
	        <mapping path="/analysis/**"/>
	        <mapping path="/member/withdraw"/>
	
	        <!-- 실행할 Interceptor -->
	        <beans:bean class="kr.or.tradelog.interceptor.LoginInterceptor"/>
	
	    </interceptor>
	</interceptors>

</beans:beans>
